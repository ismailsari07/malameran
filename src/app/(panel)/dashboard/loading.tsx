import { PanelSkeleton } from "@/components/panel/panel-skeleton";

/** Covers every customer panel screen while it resolves. */
export default function CustomerPanelLoading() {
  return <PanelSkeleton />;
}
