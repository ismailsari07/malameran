import { PanelSkeleton } from "@/components/panel/panel-skeleton";

/** Covers every admin panel screen while it resolves. */
export default function AdminPanelLoading() {
  return <PanelSkeleton />;
}
