/**
 * The Twitter card is the same image as the Open Graph one. Re-exported rather
 * than duplicated: two 1200x630 cards that could drift apart would be worse
 * than one.
 */
export { default, size, contentType, alt } from "./opengraph-image";
