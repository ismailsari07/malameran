export default function HomePage() {
  return (
    <main className="mx-auto w-full max-w-[1200px] px-5 py-16 lg:px-10">
      {/* TODO(copy): placeholder until the Home page is built in a later block. */}
      <h1 className="t-h1-page text-ink">Malameran</h1>
      <p className="t-lead text-muted mt-6">
        Managed global sourcing. One accountable party from specification to
        delivery.
      </p>
    </main>
  );
}
